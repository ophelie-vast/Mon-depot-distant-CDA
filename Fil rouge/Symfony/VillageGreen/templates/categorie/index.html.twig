{% extends 'base.html.twig' %}

{% block title %} Catégories {% endblock %}

{% block body %}

{% for row in listes %}
        <div>
            {{ row.id }}
            <a href="{{ path("categorie", { id: row.id }) }}">
                {{ row.nom }}
            </a>
        </div>
    {% endfor %}
    
{% endblock %}